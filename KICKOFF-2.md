# KICKOFF 2 — same pane, new track: `charge-auto-equilibrium` (issue #280 + the equilibrium seeker), branch `charge-equilibrium-seeker` off main 6403748 (#279 merged)

Rules as before (`_AGENT_PROTOCOL.md`: CPU rule; fresh-context Opus 5 reviews; events file `/private/tmp/claude-501/-Users-daniel-Documents-cool-eva/5373bac1-07d1-4230-9512-65440ad063f9/scratchpad/sweep/herd-events.log`, prefix `charge-equilibrium-seeker: …`; never merge; dashboard gate for any `public/` change). Model: Opus 5. Run `date` often. DEADLINE: Daniel rides at ~09:00 CEST 2026-09-19 and presses Update at key-on; the PR must be gates-green by ~07:30 CEST so the coordinator can merge before that. Daniel (verbatim): "no go ship this, if you do everything event driven you'll have it ready by tomorrow. You're much faster than you think, you can check the time every now and then. Make sure to make the most out of all the data that we have." One plan review, one diff review, both fast; no idle waiting — chain the steps. Dispatched by the coordinator on his order (real).

## The goal

Ride 54 °C "like a boss": once the pack is near the setpoint, the controller should hold the reading at 53–54 with the current within ±5 A of the session's own equilibrium, reach that equilibrium within ~10 minutes of a still reading instead of inching 2 A at a time, and never form a 55 °C train. Two parts:

1. **#280 — release on silence.** The next RAISE waits for silence — no whole-degree crossing of `batt_temp_hi` for N minutes (a statement about the bulk, τ ≈ 18.7 min, local response 1–3 min) — not for elapsed wall clock and not for a falling reading; a fast 55 → 54 return must not count as permission (31 of 50 charging crossings in the archive are the hottest-cell saw-tooth). Cuts stay unpaced (#279's measured argument stands). Size N from the post-55 data (charge-thermal's `evidence/charge-thermal/post-55.ts` / `post-55.txt` in `/Users/daniel/Documents/cool-eva/.claude/worktrees/charge-thermal/`): derated recoveries take 0.5–9.4 min (median 2.0), saw-tooth periods ~1 min.
2. **The equilibrium seeker.** Measure the session's own holding current from its holds: when the reading has been silent for ≥ N min at command I, then I is (close to) the holding current for THAT reading; the plant's dI/dT is 2.7–3.1 A/K (AMPS_PER_KELVIN = 2 is right — #275), so the raise toward 54 from a still reading T should be sized as ≈ 2 × (54 − T) A (bounded by the ceiling 75 A / rider ceiling, and by MAX_STEP_A if you keep one — argue the size), applied ONCE and then held on silence, not inched. The first move of a session, with no hold yet, uses EQUILIBRIUM_PRIOR_A = 45 (or the last hold's current if the session has one). Never raise at or above 54; the 55 veto and the hard cut when the fitted rate is high stay exactly as they are. `coolant_in` is NOT a direct input (the formula was withdrawn) — but the post-55 data says the loop's inlet temperature governs recovery: if you use it, use it only to shade the prior, and say so.

## The bar — the WHOLE archive, not today

Fixtures = open-loop replay of the rule over EVERY DC session in the archive (charge-forensics' `replay.ts` + charge-thermal's `probe-proposal.ts` show how; the decoded DBs are in those two worktrees' evidence/ — today.db + rides.db read-only + the 09-16/17 decodes charge-thermal made), with a swept tick phase (:18/:39 — sweep all 60). Assert, against the SHIPPED rule replayed the same way: (a) 55 °C crossings ≤ shipped on every session and in total; (b) on sessions with a bracketed equilibrium (today: 50–55 A at coolant 30.5; 2026-09-11: 35 A at coolant 40; the others in post-55/REPORT.md) the settled command sits within ±5 A of it; (c) time from a still reading below 54 to within ±5 A of the equilibrium ≤ 10 min; (d) the 2026-09-07 train (15 crossings in 48 min) does not reproduce — no train longer than 2. Every assertion mutation-tested on exit code against origin/main (and against #279's rule for the ones that only the seeker changes). The rule must not change what happens at/above 54 or on the descent.

## Deliverables

One PR against main: rate/pace/step/auto-curve changes (keep the files small; a `silence.ts` or `equilibrium.ts` if a file passes ~400 lines), reasons/sentences updated (dashboard gate if `public/` changes — both themes 390×844), docs/charge-auto.md with the measured argument, mutation table in the PR body, and the whole-archive replay numbers (shipped vs #279 vs this) in a table. Hand off with `charge-equilibrium-seeker: gates green, standing by — PR #<n> head <sha>` and stop. If the deadline cannot be met at full standard, post a QUESTION with what is done and what is not — do not ship a half-tested raise rule.
